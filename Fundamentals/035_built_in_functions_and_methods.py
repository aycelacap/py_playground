str()
int()
print()

print(len('hellloooo')) #9

greet = 'hellloooo'
print(greet[0:len(greet)])

# built-in function OR built-in methods

# methods or actions that are specific to a data type
# methods have a special syntax 

quote = 'to be or not to be'
print(quote.upper()) # eveything gets capitalized
print(quote.lower()) # eveything gets lowercased
print(quote.capitalize()) # only the first char of the first word will be capitalized
print(quite.find('be')) # => 3 be starts at the 3rd index

print(quote.replace('be', 'live')) # => 'to live or not to live' you can replace values
print(quote) # Whart is the output? => 'to be or not to be' => we have not reassigned that value of quote so it stays the same




