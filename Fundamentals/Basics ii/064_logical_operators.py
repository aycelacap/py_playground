# and & or are logical operators 

print(4 == 5) # False 
#  remember = is used to assign a variable

# we use == to tell the interpreter to 

print('a' > 'A') #under the hood this is a memory thing 

print(1 < 2 < 3 < 4) #True 
print(1 <= 0) #True

# < > == >= != and or

print(not(True)) # false, not creates the opposite thing